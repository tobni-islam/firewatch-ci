service: "service:svc"
labels:
  owner: islam_tb
  project: firewatch-ci

include:
  - "service/"
  - "params.yaml"

models:
  - firewatch_yolo:latest

python:
  packages:
    - ultralytics
    - opencv-python-headless
    - numpy
    - pyyaml
